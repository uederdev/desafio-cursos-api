version: '3.9'
services:
  db:
    image: postgres:14.4-alpine
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: db_cursos
    ports:
      - 5432:5432
    volumes:
      - ./db:/var/lib/postgresql/data
